# Infinite Multiplication

This project contains a C program that multiplies two positive numbers
of arbitrary length.

## Usage

Compile the program with:

```bash
gcc -Wall -Werror -Wextra -pedantic 0-mul.c _putchar.c -o mul
